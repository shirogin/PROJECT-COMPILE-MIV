CODE
    INTEGER X;
    CHAR Y1 := '5',Y2:= 'r';
    FLOAT F:=65.65;
    CONST INTEGER CX := 65,D:=20;
    CONST STRING CS:= "DEAD inside";
START
    X := CX * 20;
    WHILE (I LT J)
    EXECUTE 
    { 
        A:=2;
        A:=PROD(A+B,5+D)+D/8;
        WHILE (I LT J)
        EXECUTE 
        { 
            A:=2;
            A:=C+D/8;
            
        }
        WHEN (I LT K)
        DO
        {
            A:=C+D/8;
        }
    }
    IF()
END.